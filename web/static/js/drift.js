async function loadDrift(){try{const d=await getJSON('/api/drift');riskCritical.textContent=d.findings.filter(f=>f.severity==='CRITICAL').length;riskHigh.textContent=d.findings.filter(f=>f.severity==='HIGH').length;riskPaths.textContent=d.paths.length;findings.innerHTML=d.findings.length?d.findings.map((f,i)=>`<article class="finding-card"><div class="finding-top"><div><p class="eyebrow">${esc(f.type)}</p><div class="finding-title">Unauthorized public ingress detected</div></div><span class="badge critical">${esc(f.severity)}</span></div><div class="finding-meta"><div class="meta-box"><span>Security group</span><strong>${esc(f.security_group)}</strong></div><div class="meta-box"><span>Port</span><strong>${esc(f.port)}</strong></div><div class="meta-box"><span>Protocol</span><strong>${esc(f.protocol)}</strong></div><div class="meta-box"><span>Source</span><strong>${esc(f.source)}</strong></div></div><div class="path-item"><i data-lucide="route"></i><span>${(f.database_paths?.[0]||[]).map(esc).join(' → ')||'No path mapped'}</span></div><div class="action-row"><a class="btn btn-ghost" href="/incidents/INC-2026-${String(i+1).padStart(5,'0')}">Investigate</a><a class="btn btn-primary" href="/remediation">Generate remediation</a></div></article>`).join(''):'<div class="panel empty">✓ No drift detected. Your cloud matches the demo baseline.</div>';lucide.createIcons()}catch(e){showToast(e.message)}}driftRefresh.onclick=loadDrift;loadDrift();
