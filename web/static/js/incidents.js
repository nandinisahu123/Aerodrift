async function load(){try{const d=await getJSON('/api/incidents');incidentList.innerHTML=d.length?d.map(x=>`<article class="incident-row"><div class="incident-icon"><i data-lucide="siren"></i></div><div class="incident-main"><span class="eyebrow">${esc(x.id)}</span><h3>${esc(x.title)}</h3><p>${esc(x.resource)} · ${esc(x.type)}</p></div><span class="badge critical">${esc(x.severity)}</span><span class="badge">${esc(x.status)}</span><a class="icon-btn" href="/incidents/${esc(x.id)}"><i data-lucide="arrow-up-right"></i></a></article>`).join(''):'<div class="panel empty">No active incidents.</div>';lucide.createIcons()}catch(e){showToast(e.message)}}load();
